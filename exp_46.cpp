#include<stdio.h>
#include<math.h>

int main()
{
    int i,j,n,v,f,c=0;

    scanf("%d", &n);

    for(i=2; i<=n; i+=)
    {
        v=sqrt(i);
        f=1;

        for(j=2; j<=v; j++)
        {
            if
        }
    }
}
